mod passport_record;
mod passport_record_attribute_key;
mod passport_record_batch;
mod validated_passport_record;

/// A collection of passport records.
pub type PassportRecordBatch = passport_record_batch::PassportRecordBatch;
